extends layout

block content
  h1 #{title}: #{bookinst.imprint}
  
  
    p Do you really want to delete this bookinstance?
    
    form(method='POST' action='')
      div.form-group
        input#bookinstanceid.form-control(type='hidden',name='bookinstanceid', required='true', value=bookinst._id )

      button.btn.btn-primary(type='submit') Delete